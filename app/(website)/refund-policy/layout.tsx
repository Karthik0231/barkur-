import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Refund Policy | Sri Kalikamba Temple",
  description:
    "Refund and cancellation policy for Sri Kalikamba Temple — seva bookings, donations, and payment processing.",
}

export default function RefundPolicyLayout({ children }: { children: React.ReactNode }) {
  return children
}
